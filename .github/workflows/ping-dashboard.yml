name: Keep dashboard alive
on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch: 
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping dashboard
        run: |
          curl https://app-gitetl.streamlit.app
